import {View, Text} from 'react-native';
import React from 'react';

const TitleHeader = ({onBack, title, share}) => {
  return (
    <View>
      <Text>TitleHeader</Text>
    </View>
  );
};

export default TitleHeader;
